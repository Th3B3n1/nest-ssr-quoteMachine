<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div style="display: flex; flex-direction: row; align-items: center;">
        <p><%= data[0] %>
        <% for(let i = 1; i < data.length; i++) { %>
            <b><%= highlight %></b><%= data[i] || "" %></p>
        <% } %>
    </div>
</body>
</html>